# FocusBook GitHub Actions Workflow Diagram

## 🎨 Visual Guide to Your Build Process

### 1. Overview: The Big Picture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         YOU (Developer)                              │
│                                                                      │
│  Write Code → git push → GitHub → GitHub Actions → Release         │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      GITHUB ACTIONS                                  │
│  ┌───────────┐    ┌──────────────┐    ┌─────────────────────┐     │
│  │  Lint     │───▶│  Build       │───▶│  Release            │     │
│  │  (Quick)  │    │  (3 OS)      │    │  (On tags only)     │     │
│  │  30 sec   │    │  15 min      │    │  30 sec             │     │
│  └───────────┘    └──────────────┘    └─────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    READY-TO-INSTALL APPS                            │
│                                                                      │
│  Windows: focusbook-setup.exe                                       │
│  macOS:   FocusBook-1.0.0.dmg                                       │
│  Linux:   FocusBook-1.0.0.AppImage                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 2. Detailed Workflow Flow

```
                    ┌────────────────────┐
                    │   GitHub Trigger   │
                    │  (push/tag/PR)     │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │   Concurrency      │
                    │   Check            │
                    │  (Cancel old runs) │
                    └─────────┬──────────┘
                              │
                    ┌─────────▼──────────┐
                    │   Job 1: Lint      │
                    │   ┌──────────────┐ │
                    │   │ Checkout code│ │
                    │   │ Install Node │ │
                    │   │ npm ci       │ │
                    │   │ ESLint check │ │
                    │   └──────┬───────┘ │
                    └──────────┼─────────┘
                              ✓
                              │
              ┌───────────────┼───────────────┐
              │               │               │
    ┌─────────▼─────┐  ┌─────▼──────┐  ┌────▼──────┐
    │ Job 2a: Build │  │Job 2b:Build│  │Job 2c:    │
    │   (Windows)   │  │  (macOS)   │  │ Build     │
    │               │  │            │  │ (Linux)   │
    └───────┬───────┘  └──────┬─────┘  └────┬──────┘
            │                 │              │
            │    All run in parallel         │
            │    (same steps, different OS)  │
            │                 │              │
    ┌───────▼─────────────────▼──────────────▼──────┐
    │     Each Build Job Does:                      │
    │                                                │
    │  1. Setup Environment                         │
    │     ├─ Checkout code                          │
    │     ├─ Install Node.js 20 (cached)            │
    │     ├─ Install Python 3.11 (cached)           │
    │     └─ Install Linux libs (Linux only)        │
    │                                                │
    │  2. Install Dependencies                      │
    │     ├─ npm ci (Node packages)                 │
    │     └─ pip install (Python packages)          │
    │                                                │
    │  3. Build AI Service                          │
    │     ├─ Run build_service.py                   │
    │     ├─ PyInstaller creates executable         │
    │     └─ Output: AI_agent/dist/ai_service.exe   │
    │                                                │
    │  4. Rebuild Native Modules                    │
    │     └─ npm rebuild (sqlite3, etc.)            │
    │                                                │
    │  5. Build Electron App                        │
    │     ├─ electron-vite build                    │
    │     └─ Output: out/ folder                    │
    │                                                │
    │  6. Package App                               │
    │     ├─ electron-builder --win/mac/linux       │
    │     └─ Output: dist/ folder                   │
    │                                                │
    │  7. Upload Artifacts                          │
    │     └─ Save to GitHub (30 days)               │
    └────────────────┬───────────────────────────────┘
                     │
                     │ (Only if tag v*)
                     │
            ┌────────▼─────────┐
            │  Job 3: Release  │
            │                  │
            │  1. Download all │
            │     artifacts    │
            │  2. Create       │
            │     GitHub       │
            │     Release      │
            │  3. Upload       │
            │     installers   │
            └──────────────────┘
```

---

### 3. Build Job Detailed Steps (Per OS)

```
┌─────────────────────────────────────────────────────────────┐
│              BUILD JOB (Windows/macOS/Linux)                │
└─────────────────────────────────────────────────────────────┘

Step 1: Setup Environment
┌─────────────────────────────────────────────────────────────┐
│ actions/checkout@v4                                         │
│ └─ Downloads your repository to /home/runner/work/focusbook│
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ actions/setup-node@v4                                       │
│ └─ Installs Node.js 20                                      │
│    └─ Checks cache (npm packages)                           │
│       ├─ Hit: Restore packages (30 sec)                     │
│       └─ Miss: Download packages (5 min)                    │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ actions/setup-python@v5                                     │
│ └─ Installs Python 3.11                                     │
│    └─ Checks cache (pip packages)                           │
│       ├─ Hit: Restore packages (20 sec)                     │
│       └─ Miss: Download packages (3 min)                    │
└─────────────────────────────────────────────────────────────┘

Step 2: Install Dependencies
┌─────────────────────────────────────────────────────────────┐
│ npm ci                                                       │
│ └─ Reads: package.json + package-lock.json                  │
│    └─ Installs: electron, react, tailwind, etc. (50+ pkgs) │
│       └─ Time: 30 sec (with cache) / 5 min (without)       │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ pip install -r AI_agent/requirements.txt                    │
│ └─ Installs: langchain, fastapi, pyinstaller, etc.         │
│    └─ Time: 20 sec (with cache) / 3 min (without)          │
└─────────────────────────────────────────────────────────────┘

Step 3: Build AI Service
┌─────────────────────────────────────────────────────────────┐
│ npm run build-ai-service                                    │
│ └─ Calls: cd AI_agent && python build_service.py           │
│    └─ Runs: pyinstaller ai_service.spec                    │
│       └─ Creates: AI_agent/dist/ai_service.exe             │
│          ├─ Bundles: Python runtime                         │
│          ├─ Bundles: All Python packages                    │
│          ├─ Includes: start_service.py                      │
│          ├─ Includes: app.py                                │
│          ├─ Includes: langgraph_mcp_client.py              │
│          └─ Includes: math_mcp_server.py                   │
│    └─ Time: 2-3 minutes                                     │
└─────────────────────────────────────────────────────────────┘

Step 4: Rebuild Native Modules
┌─────────────────────────────────────────────────────────────┐
│ npm run rebuild-native                                      │
│ └─ Calls: npm rebuild                                       │
│    └─ Recompiles for current OS:                            │
│       ├─ sqlite3 (native database bindings)                 │
│       ├─ electron-active-window (window monitoring)         │
│       └─ Other native modules                               │
│    └─ Time: 1-2 minutes                                     │
└─────────────────────────────────────────────────────────────┘

Step 5: Build Electron App
┌─────────────────────────────────────────────────────────────┐
│ npm run build                                               │
│ └─ Calls: electron-vite build                               │
│    └─ Compiles:                                             │
│       ├─ src/main/ → out/main/ (Electron main process)     │
│       ├─ src/preload/ → out/preload/ (Preload scripts)     │
│       └─ src/renderer/ → out/renderer/ (React app)         │
│          └─ Vite optimizes:                                 │
│             ├─ Minifies JavaScript                          │
│             ├─ Optimizes images                             │
│             ├─ Tree-shakes unused code                      │
│             └─ Bundles dependencies                         │
│    └─ Time: 2-3 minutes                                     │
└─────────────────────────────────────────────────────────────┘

Step 6: Package App
┌─────────────────────────────────────────────────────────────┐
│ npx electron-builder --win/mac/linux                        │
│ └─ Reads: electron-builder.yml                              │
│    └─ Packages:                                             │
│       ├─ Files from: out/ (your app)                        │
│       ├─ ExtraResources:                                    │
│       │  ├─ AI_agent/dist/ai_service.exe                    │
│       │  ├─ scripts/get_active_url.py                       │
│       │  └─ scripts/closeTab.py                             │
│       ├─ Electron runtime                                   │
│       ├─ Node.js runtime                                    │
│       └─ Native modules                                     │
│    └─ Creates installer:                                    │
│       ├─ Windows: dist/focusbook-setup.exe (NSIS)          │
│       ├─ macOS: dist/FocusBook-1.0.0.dmg                   │
│       └─ Linux: dist/FocusBook-1.0.0.AppImage              │
│    └─ Time: 3-5 minutes                                     │
└─────────────────────────────────────────────────────────────┘

Step 7: Upload Artifacts
┌─────────────────────────────────────────────────────────────┐
│ actions/upload-artifact@v4                                  │
│ └─ OS-specific upload:                                      │
│    ├─ Windows: Upload dist/*.exe, dist/*.msi               │
│    ├─ macOS: Upload dist/*.dmg, dist/*.zip                 │
│    └─ Linux: Upload dist/*.AppImage, *.deb, *.snap         │
│ └─ Stored on GitHub for 30 days                            │
│ └─ Time: 1-2 minutes                                        │
└─────────────────────────────────────────────────────────────┘
```

---

### 4. File Dependencies Map

```
┌──────────────────────────────────────────────────────────────┐
│                   FILE DEPENDENCY GRAPH                       │
└──────────────────────────────────────────────────────────────┘

.github/workflows/build.yml
    │
    ├─ Triggers on: push/tag/PR
    │
    ├─ Uses: package.json
    │    │
    │    ├─ "build": "electron-vite build"
    │    │    └─ Compiles: src/ → out/
    │    │
    │    ├─ "build-ai-service": "cd AI_agent && python build_service.py"
    │    │    │
    │    │    └─ Uses: AI_agent/build_service.py
    │    │         │
    │    │         └─ Uses: AI_agent/ai_service.spec
    │    │              │
    │    │              ├─ Reads: AI_agent/start_service.py
    │    │              ├─ Reads: AI_agent/app.py
    │    │              ├─ Reads: AI_agent/langgraph_mcp_client.py
    │    │              └─ Reads: AI_agent/math_mcp_server.py
    │    │
    │    └─ "rebuild-native": "npm rebuild"
    │         └─ Recompiles: sqlite3, electron-active-window
    │
    ├─ Uses: AI_agent/requirements.txt
    │    └─ Installs: langchain, fastapi, pyinstaller, etc.
    │
    └─ Uses: electron-builder.yml
         │
         ├─ files: ["out/**/*"]
         │    └─ From: npm run build
         │
         ├─ extraResources:
         │    ├─ from: AI_agent/dist → to: ai_service
         │    │    └─ From: npm run build-ai-service
         │    │
         │    └─ from: scripts → to: scripts
         │         ├─ get_active_url.py
         │         └─ closeTab.py
         │
         └─ Creates: dist/focusbook-setup.exe (and others)
```

---

### 5. Time Breakdown (Per OS Build)

```
┌────────────────────────────────────────────────────────────┐
│             TIMING BREAKDOWN (With Caching)                │
└────────────────────────────────────────────────────────────┘

Total: ~15 minutes per OS

┌─────────────────────────────────────┬──────────────────────┐
│ Step                                │ Time                 │
├─────────────────────────────────────┼──────────────────────┤
│ Setup Environment                   │                      │
│  ├─ Checkout code                   │ 10s                  │
│  ├─ Setup Node.js                   │ 5s                   │
│  ├─ Setup Python                    │ 5s                   │
│  └─ Install Linux libs (Linux only) │ 20s                  │
├─────────────────────────────────────┼──────────────────────┤
│ Install Dependencies                │                      │
│  ├─ npm ci (with cache)             │ 30s                  │
│  └─ pip install (with cache)        │ 20s                  │
├─────────────────────────────────────┼──────────────────────┤
│ Build AI Service                    │ 2m 30s               │
├─────────────────────────────────────┼──────────────────────┤
│ Rebuild Native Modules              │ 1m 30s               │
├─────────────────────────────────────┼──────────────────────┤
│ Build Electron App                  │ 2m 30s               │
├─────────────────────────────────────┼──────────────────────┤
│ Package App                         │ 5m 0s                │
├─────────────────────────────────────┼──────────────────────┤
│ Upload Artifacts                    │ 1m 30s               │
└─────────────────────────────────────┴──────────────────────┘

Without caching: Add +8 minutes for dependency downloads
```

---

### 6. Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                   DATA FLOW IN BUILD                         │
└─────────────────────────────────────────────────────────────┘

Your Code (GitHub)
    │
    ├── src/
    │   ├── main/ ────────────────┐
    │   ├── preload/ ──────────┐  │
    │   └── renderer/ ──────┐  │  │
    │                        │  │  │
    ├── AI_agent/            │  │  │
    │   ├── *.py ───────┐   │  │  │
    │   └── requirements.txt│  │  │
    │                    │   │  │  │
    └── scripts/         │   │  │  │
        └── *.py ────────┼───┼──┼──┼─────┐
                         │   │  │  │     │
                         ▼   ▼  ▼  ▼     ▼
                    ┌───────────────────────┐
                    │   BUILD PROCESS       │
                    └───────────┬───────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
        ┌───────────┐   ┌──────────┐   ┌──────────┐
        │ Python    │   │ Electron │   │ Scripts  │
        │ Compiled  │   │ Compiled │   │ Copied   │
        └─────┬─────┘   └─────┬────┘   └────┬─────┘
              │               │              │
              ▼               ▼              ▼
        ┌────────────────────────────────────────┐
        │      AI_agent/dist/ai_service.exe      │
        │      out/main/                         │
        │      out/preload/                      │
        │      out/renderer/                     │
        │      scripts/                          │
        └────────────────┬───────────────────────┘
                         │
                         ▼
                ┌────────────────┐
                │ electron-builder│
                └────────┬────────┘
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
    Windows          macOS            Linux
    .exe             .dmg             .AppImage
    125 MB           140 MB           135 MB
```

---

### 7. Cache Strategy Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    CACHING STRATEGY                          │
└─────────────────────────────────────────────────────────────┘

First Build (No Cache)
┌─────────────────────────────────────────────────────────────┐
│ npm ci                                                       │
│ └─ Download 500 MB packages ──────────── 5 minutes          │
│    └─ Save to GitHub Cache ────────────┐                    │
│                                         │                    │
│ pip install                             │                    │
│ └─ Download 150 MB packages ────────── 3 minutes           │
│    └─ Save to GitHub Cache ────────────┤                    │
│                                         │                    │
│ Total dependency time: 8 minutes        │                    │
└─────────────────────────────────────────┼──────────────────┘
                                          │
                            ┌─────────────▼────────────┐
                            │   GitHub Cache Storage   │
                            │  ├─ npm packages (500MB) │
                            │  └─ pip packages (150MB) │
                            └─────────────┬────────────┘
                                          │
Second Build (With Cache)                │
┌─────────────────────────────────────────┼──────────────────┐
│ npm ci                                  │                   │
│ └─ Restore from cache ◄─────────────────┘                   │
│    └─ Time: 30 seconds                                      │
│                                                              │
│ pip install                                                  │
│ └─ Restore from cache ◄──────────────────────────────────── │
│    └─ Time: 20 seconds                                      │
│                                                              │
│ Total dependency time: 50 seconds                           │
│ Time saved: 7 minutes! ✨                                   │
└─────────────────────────────────────────────────────────────┘

Cache Invalidation:
├─ npm cache: Invalidated when package-lock.json changes
└─ pip cache: Invalidated when requirements.txt changes
```

---

### 8. Artifact vs Release Comparison

```
┌─────────────────────────────────────────────────────────────┐
│          ARTIFACTS vs RELEASES                               │
└─────────────────────────────────────────────────────────────┘

ARTIFACTS (Every build)
┌────────────────────────────────────────────────────────────┐
│ Trigger:   Any push to main                                │
│ Storage:   GitHub Actions artifacts                        │
│ Lifetime:  30 days (auto-deleted)                          │
│ Access:    Only you (repository collaborators)             │
│ Purpose:   Testing builds before release                   │
│                                                             │
│ Example:                                                    │
│ You push → Build runs → Artifacts created                  │
│ You download → Test → Find bug → Fix → Push again         │
└────────────────────────────────────────────────────────────┘

RELEASES (Tagged versions only)
┌────────────────────────────────────────────────────────────┐
│ Trigger:   Tag push (v1.0.0)                               │
│ Storage:   GitHub Releases                                 │
│ Lifetime:  Permanent                                       │
│ Access:    Everyone (public)                               │
│ Purpose:   Official releases for users                     │
│                                                             │
│ Example:                                                    │
│ git tag v1.0.0 → Build runs → Release created             │
│ Users download from GitHub Releases page                   │
└────────────────────────────────────────────────────────────┘

Workflow:
Testing phase:     Use Artifacts (push to main)
Ready to release:  Create Tag (push v1.0.0)
```

---

### 9. Error Recovery Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  ERROR HANDLING FLOW                         │
└─────────────────────────────────────────────────────────────┘

Build Starts
    │
    ▼
Lint Job
    │
    ├─ Success ─────────────► Continue to Build Jobs
    │
    └─ Failure ────────────┐
         │                 │
         ▼                 │
    🔴 Stop workflow       │
         │                 │
         │                 │
    ┌────▼──────────────┐  │
    │ You see error:    │  │
    │ "ESLint failed"   │  │
    │                   │  │
    │ Fix locally:      │  │
    │ npm run lint      │  │
    │                   │  │
    │ Push fix          │  │
    └────┬──────────────┘  │
         │                 │
         └─────────────────┘
              │
              ▼
         Retry build


Build Job
    │
    ├─ Python deps fail ────┐
    │                        │
    │                        ▼
    │                   🔴 Check requirements.txt
    │                        │
    │                        ├─ Missing pyinstaller?
    │                        ├─ Wrong version?
    │                        │
    │                        └─ Fix → Push → Retry
    │
    ├─ Native rebuild fail ──┐
    │                         │
    │                         ▼
    │                    🔴 Check package.json
    │                         │
    │                         └─ Add rebuild-native script
    │
    └─ Package fail ──────────┐
                               │
                               ▼
                          🔴 Check electron-builder.yml
                               │
                               ├─ Wrong paths?
                               ├─ Missing files?
                               │
                               └─ Fix → Push → Retry
```

---

**This visual guide shows exactly how everything connects in your FocusBook GitHub Actions workflow!** 🎨

*Generated for FocusBook v1.0.0*
